# Парсится в один файл, оттуда берется последние несколько дней, анализируется и рендерится вывод

# Переход на последнюю страницу прост. Там же по пагинации ищется номер этой последней страницы.
# В функции таймера будет расчет кол-ва страниц, будет переписывание номера последней скачанной страницы. В самой функции скачивания поставить sleep(20 секунд + рандом), чтобы не нагружать сервер. И запись в файл CSV будет идти вся страница, потом файл закрывается до следующего открытия.

# Сейчас парсинг смартлаба настраиваем (скорее всего в ту же функцию), потом формирование токенов (вопрос - надо ли эти токены в отдельный файл выгружать), потом принт того к чему склоняются форумы, из нейронки



вообще крутая тема этот Vim.

есть похоже инструмент чтобы во всем коде во всех файлах питона поменять смарт на другое, и мфд...
.
